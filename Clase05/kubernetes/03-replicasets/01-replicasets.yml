apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: rs-nginx
spec:
  replicas: 5
  selector:
    matchLabels:
      env: dev
      app: frontend
  template:
    metadata:
      labels:
        env: dev
        app: frontend
    spec:
      containers:
        - name: container-python
          image: python:3.7-alpine
          command: [
            "sh",
            "-c",
            "echo 'página creada desde python' > index.html && python -m http.server 8080"
          ]